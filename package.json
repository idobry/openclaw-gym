{
  "name": "openclaw-gym",
  "private": true,
  "workspaces": ["phone", "vercel"],
  "scripts": {
    "dev:api": "npm run dev:api -w vercel",
    "dev:web": "npm run dev -w vercel",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test:smoke": "vitest run tests/smoke.test.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "vitest": "^3.0.0"
  }
}
